export const mode = {
  options: ['dark', 'light'],
  control: { type: 'inline-radio' },
};

export const siteKey = {
  options: ['atk', 'cco', 'cio'],
  control: { type: 'inline-radio' },
};

export default {
  mode,
  siteKey,
};
