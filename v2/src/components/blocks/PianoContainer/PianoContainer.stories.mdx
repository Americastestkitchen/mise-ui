import { Canvas, Meta, Story } from "@storybook/addon-docs";
import { withDesign } from "storybook-addon-designs";
import { PianoContainer, PianoContainerProps } from "./PianoContainer";
import { PianoStoryWrapper } from "./PianoStoryWrapper";

<Meta
  title="Components/Blocks/PianoContainer"
  component={PianoContainer}
/>

export const Template = (args) => <PianoContainer {...args} />
export const TemplateWithContainer = (args) => (
  <PianoStoryWrapper>
    <PianoContainer {...args} />
  </PianoStoryWrapper>
);

# PianoContainer

<Canvas>
  <Story
    name="Sandbox"
    args={{
      containerSelector: 'mise piano-sandbox',
      segment: 'anonymous',
      tags: 'default',
      url: '/sandbox?mise=true',
    }}
  >
    {TemplateWithContainer.bind({})}
  </Story>

  <Story 
    name="PianoMarketingHat Thanksgiving"
    args={{
      containerSelector: 'piano-marketing-hat thanksgiving-landing',
      segment: 'anonymous',
      tags: 'default',
      url: '/thanksgiving'
    }}
  >
    {Template.bind({})}
  </Story>

  <Story 
    name="PianoMarketingHat Homepage"
    args={{
      containerSelector: 'piano-marketing-hat reviews-landing-page',
      segment: 'anonymous',
      tags: 'default',
      url: '/reviews'
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>
