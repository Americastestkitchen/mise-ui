import { Canvas, Meta, Typeset } from "@storybook/addon-docs";

import { ZIndexes } from "../utils/TokenObjects.ts";

import { getTokenTitle, tokenParams } from "../utils/scripts/utils";

import KeyValuePreview from "../utils/KeyValuePreview/KeyValuePreview";

<Meta
  title="Tokens/Z-Indexes"
  parameters={{...tokenParams}}
/>

# Z-Indexes

<br/>

## For Developers

Z-index styling is located in the `./v2/src/styles/_tokens/_layout.scss` and is part of `mise.scss`.

### Using Z-Indexes

Z-indexes can be used with the `mise.zIndex()` function. This custom SASS function takes a `level` and optional `adjustment` argument. `adjustment` can be anywhere between `1` and `99`, and allows for indexes to exist on the same level with a heirarchy, e.g. 101, 102. 103, etc…

#### SASS | *.scss

```
@use "./src/styles/mise.scss"

.example {
  z-index: mise.color(page); // returns 100

  &__adjusted {
    z-index: mise.color(page, 1); // returns 101
  }
}
```

## Levels

<KeyValuePreview
  keyLabel="Level"
  keyColumnWidth="10rem"
  valueLabel="Index"
  list={ZIndexes}
/>