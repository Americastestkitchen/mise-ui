import { Canvas, Meta, Typeset } from "@storybook/addon-docs";

import { Breakpoints } from "../utils/TokenObjects.ts";

import { tokenParams, remAndPx, remToPx } from "../utils/scripts/utils";

import KeyValuePreview from "../utils/KeyValuePreview/KeyValuePreview";

<Meta
  title="Tokens/Breakpoints"
  parameters={{...tokenParams}}
/>

# Breakpoints

<br/>

## For Designers

!!! **TODO:** EMBED FIGMA DOCUMENTATION HERE !!!

## For Developers

Breakpoint styling is located in the `./v2/src/styles/_tokens/_layout.scss` and is part of `mise.scss`.

### Using Breakpoints

Breakpoints can be used with the `mise.breakpoint()` function. This custom SASS function takes a `breakpoint` argument.

> **Note:** Media Queries should always set breakpoints with `min-width`.

#### SASS | *.scss

```
@use "./src/styles/mise.scss"

.example {
  @media only screen and (min-width: mise.breakpoint(sm)) {
    ...Styles
  }
}
```

## Ranges

<KeyValuePreview
  list={Breakpoints}
  keyLabel="Breakpoint"
  keyColumnWidth="11rem"
  valueLabel="Min-Width"
  valueColumnWidth="12rem"
  valueFormat={remAndPx}
  previewLabel="Range"
  previewFormat={(list, key, value, index, array) => {
    const rangeEnd = list[array[index + 1]] ? ` - ${Number(remToPx(String(list[array[index + 1]])).split('px').shift()) - 1}px` : " +";
    return `${remToPx(value)}${rangeEnd}`
  }}
/>
