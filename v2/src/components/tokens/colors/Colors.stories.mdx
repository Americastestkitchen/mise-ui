import { Meta, ColorPalette, ColorItem } from "@storybook/addon-docs";
import { SourceColors, Colors } from "./Colors";
import { getTokenTitle, tokenParams } from '../utils'

<Meta
  title="Tokens/Colors"
  parameters={{...tokenParams}}
/>

# Colors

<br/>

## For Designers

!!! **TODO:** EMBED FIGMA DOCUMENTATION HERE !!!

## For Developers

Color styling is located in the `./v2/src/styles/_tokens/_colors.scss` and is part of `mise.scss`.

### Using the Color Palette

Colors can be used with the `mise.color()` function. This custom SASS function takes a `category` and optional `hue`, and `variant` arguments. All colors should be implemented with `mise.color()`, **no hex codes, rgba, sass transforms, etc…** should be used directly in component styling. 

> **Note:** All colors have the `variant`: `base`, the default color. `hues` vary by `category`, see the [color palette](#color-palette) below for available options.

#### SASS | *.scss

```
@use "./src/styles/mise.scss"

// Example: mise.color(category, hue, variant)

.example {
  background-color: mise.color(primary, mid, base);
  border-color: mise.color(primary, dark, base);
  color: mise.color(netural, light, tint);

  &:hover {
    background-color: mise.color(primary, mid, shade);
    border-color: mise.color(primary, dark, shade);
  }
}
```

## <a name="color-palette" />Color Palette

The following colors are **the only colors that can be used** to style Mise components.

{Object.keys(Colors).map((category, i) => {
  return (
    <>
      {i === 0 ? "" : <hr />}
      <h3><a name={category} />{getTokenTitle(category)}</h3>
      <ColorPalette>
        {Object.keys(Colors[category]).map((hue) => {
          return <ColorItem title={getTokenTitle(hue)} subtitle={hue} colors={Colors[category][hue]} />
        })}
      </ColorPalette>
    </>
  )
})}

## Source Colors

The following color palette is for **reference only**. These represent the source color values that are mapped to the color palatte and `mise.color()` and should not be used directly in code.

<ColorPalette>
  {Object.keys(SourceColors).map((key, i) => {
    return <ColorItem title={getTokenTitle(key)} colors={{ [key]: SourceColors[key] }} />;
  })}
</ColorPalette>
