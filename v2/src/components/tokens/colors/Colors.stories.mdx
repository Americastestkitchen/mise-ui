import { Meta, ColorPalette, ColorItem } from "@storybook/addon-docs";
import { SourceColors, Colors } from "./Colors";
import { getTokenTitle, tokenParams } from '../utils'

<Meta
  title="Tokens/Colors"
  parameters={tokenParams}
/>

# Colors

## For Designers

!!! **TODO:** EMBED FIGMA DOCUMENTATION HERE !!!

## For Developers

### Using the Color Palette

Colors can be used with the `mise.color()` function. This custom SASS function takes a `hue` and optional `color`, and `variant` arguments. All colors should be implemented with `mise.color()`, **no hex codes, rgba, sass transforms, etc…** should be used directly in component styling. 

> **Note:** All colors have the `variant`: `base`, and all hues have the `color`: `mid`, **excluding Editorial** colors which are numbered, starting with `color1`.

#### SASS | *.scss

```
@use "./src/styles/mise.scss"

.example {
  background-color: mise.color(brand, mid, base);
  border-color: mise.color(brand, dark, base);
  color: mise.color(netural, white, base);

  &:hover {
    background-color: mise.color(brand, mid, shade);
    border-color: mise.color(brand, dark, shade);
  }
}
```

## Color Palette

The following colors are **the only colors that can be used** to style Mise components.

### Primary

<ColorPalette>
  {Object.keys(Colors.primary).map((key, i) => {
    return <ColorItem title={getTokenTitle(key)} subtitle={key} colors={Colors.primary[key]} />;
  })}
</ColorPalette>

---

### Accent

<ColorPalette>
  {Object.keys(Colors.accent).map((key, i) => {
    return <ColorItem title={getTokenTitle(key)} subtitle={key} colors={Colors.accent[key]} />;
  })}
</ColorPalette>

---

### Marketing

<ColorPalette>
  {Object.keys(Colors.marketing).map((key, i) => {
    return <ColorItem title={getTokenTitle(key)} subtitle={key} colors={Colors.marketing[key]} />;
  })}
</ColorPalette>

---

### Editorial

<ColorPalette>
  {Object.keys(Colors.editorial).map((key, i) => {
    return <ColorItem title={getTokenTitle(key)} subtitle={key} colors={Colors.editorial[key]} />;
  })}
</ColorPalette>

---

### Neutral

<ColorPalette>
  {Object.keys(Colors.neutral).map((key, i) => {
    return <ColorItem title={getTokenTitle(key)} subtitle={key} colors={Colors.neutral[key]} />;
  })}
</ColorPalette>

## Source Color Palette

The following color palette is for **reference only**. These represent the source color values and names that are mapped to `mise.color()` and should not be used directly in code.

<ColorPalette>
  {Object.keys(SourceColors).map((key, i) => {
    return <ColorItem title={getTokenTitle(key)} colors={{ [key]: SourceColors[key] }} />;
  })}
</ColorPalette>
