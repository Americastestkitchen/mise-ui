import { Canvas, Meta, Typeset } from "@storybook/addon-docs";
import { Breakpoints, SpacingUnit, Spacing, Content, Grid } from "./Layout.ts";
import { getTokenTitle, tokenParams } from '../utils'

<Meta
  title="Tokens/Layout/Breakpoints"
  parameters={{...tokenParams}}
/>

# Breakpoints

<table style={{textAlign: "left", width: "100%"}}>
  <tr>
    <th>Key</th>
    <th>rem</th>
    <th>Pixel Range</th>
  </tr>
  <tr>
    <td>base</td>
    <td>0</td>
    <td>0 - {Breakpoints.sm}</td>
  </tr>
  {Object.keys(Breakpoints).map((key, i, arr) => {
    return (
      <tr>
        <td>
          {key}
        </td>
        <td>
          {Breakpoints[key]}
        </td>
        <td>
          {i + 1 < arr.length ? `${Breakpoints[key]} - ${Breakpoints[arr[i + 1]]}` :  Breakpoints[key]}
        </td>
      </tr>
    )
  })}
</table>

## For Designers

> **TODO:** EMBED FIGMA DOCUMENTATION HERE

## For Developers

Layout styling is located in the `./v2/src/styles/_tokens/_layout.scss` and is part of `mise.scss`.

### Using Breakpoints

#### SASS | *.scss

```
@use "./src/styles/mise.scss"

.example {
  background-color: mise.color(brand, mid, base);
  border-color: mise.color(brand, dark, base);
  color: mise.color(netural, white, base);

  &:hover {
    background-color: mise.color(brand, mid, shade);
    border-color: mise.color(brand, dark, shade);
  }
}
```

## Spacing

**Spacing Unit:** {SpacingUnit}

{Object.keys(Spacing).map((key, i) => {
  return (
    <div style={{borderBottom: "1px solid #eee", marginBottom: '1rem', paddingBottom: '1rem'}}>
      <span style={{display: 'inline-block', width: '3rem'}}><strong>{key}:</strong></span>
      <span style={{display: 'inline-block', marginLeft: "1rem", width: '4rem'}}>{Spacing[key]}</span>
      <span style={{backgroundColor: "red", display: 'inline-block', height: Spacing[key], marginLeft: "2rem", width: Spacing[key]}}></span>
    </div>
  )
})}

## For Designers

> **TODO:** EMBED FIGMA DOCUMENTATION HERE

## For Developers

Layout styling is located in the `./v2/src/styles/_tokens/_layout.scss` and is part of `mise.scss`.

### Using Spacing

#### SASS | *.scss

```
@use "./src/styles/mise.scss"

.example {
  background-color: mise.color(brand, mid, base);
  border-color: mise.color(brand, dark, base);
  color: mise.color(netural, white, base);

  &:hover {
    background-color: mise.color(brand, mid, shade);
    border-color: mise.color(brand, dark, shade);
  }
}
```
## Content Container

**Max Width:** {Content.maxWidth}

### Gutter per Breakpoint

{Object.keys(Content.gutter).map((key, i) => {
  return (
    <p>
      <strong>{key}:</strong> {Content.gutter[key]}
    </p>
  )
})}

## Grid

### Column Widths

{Object.keys(Grid.column).map((key, i) => {
  return (
    <p>
      <strong>{key}:</strong> {Grid.column[key]}
    </p>
  )
})}

### Gap per Breakpoint

{Object.keys(Grid.gap).map((key, i) => {
  return (
    <p>
      <strong>{key}:</strong> {Grid.gap[key]}
    </p>
  )
})}