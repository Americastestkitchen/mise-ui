import { Canvas, Meta, Typeset } from "@storybook/addon-docs";

import { ContentContainer } from "./Layout.ts";

import { getTokenTitle, tokenParams } from "../utils/scripts/utils";

<Meta
  title="Tokens/Layout/Grid/Usage"
  parameters={{...tokenParams}}
/>

# Grid

<br/>

## For Designers

!!! **TODO:** EMBED FIGMA DOCUMENTATION HERE !!!

## For Developers

Content container styling is located in the `./v2/src/styles/_tokens/_layout.scss` and is part of `mise.scss`.

### Using Grid Components

#### GridRow

DOC HERE
alignment
wrap

#### GridColumn

DOC HERE
width

**TSX | \*.tsx**

```
@import { Row, Column } from "./mise/v2/src/components/Tokens/Layout/Grid"

export const Example: React.FC = () => {
  return (
    <GridRow>
      <GridColumn width="half">
        ...Children
      </GridColumn>
      <GridColumn width="half">
        ...Children
      </GridColumn>
    </GridRow>
  );
};
```

## Grid Styling

max width: {ContentContainer.maxWidth}

{Object.keys(ContentContainer.gutter).map((key, i) => {
  return <p>{key}: {ContentContainer.gutter[key]}</p>
})}